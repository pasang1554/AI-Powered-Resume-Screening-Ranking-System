services:
  smart-resume-matcher:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./smartresume.db:/app/smartresume.db
    restart: always
